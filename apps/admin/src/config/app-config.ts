// Imported by client components, so keep this free of server-only values and of
// package.json — importing it here ships the whole file, devDependencies included.
export const APP_CONFIG = {
  name: "Admin",
  meta: {
    title: "Admin",
    description: "Admin dashboard.",
  },
};
